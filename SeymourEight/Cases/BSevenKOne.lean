import SeymourEight.Cases.BSevenKOne.Result

set_option linter.style.header false

/-!
# The `(|B|, k) = (7, 1)` development

The imported theorem assembles all parameter branches of the `(7,1)` case.
-/
